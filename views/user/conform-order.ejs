<style>
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f5f5f5;
        margin: 0;
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        overflow: hidden;
    }

    .success-container {
        text-align: center;
        background-color: white;
        padding: 3em;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        transform: scale(0);
        animation: scale-in 0.5s forwards ease-out;
        max-width: 480px;
        width: 90%;
    }

    @keyframes scale-in {
        to {
            transform: scale(1);
        }
    }

    .checkmark-container {
        display: flex;
        justify-content: center;
        margin-bottom: 2em;
    }

    .checkmark {
        width: 80px;
        height: 80px;
    }

    .checkmark__circle {
        stroke: #4CAF50;
        stroke-width: 4;
        stroke-dasharray: 166;
        stroke-dashoffset: 166;
        stroke-linecap: round;
        animation: draw-circle 1s ease-out forwards;
    }

    .checkmark__check {
        stroke: #4CAF50;
        stroke-width: 4;
        stroke-dasharray: 48;
        stroke-dashoffset: 48;
        stroke-linecap: round;
        animation: draw-check 1s 0.5s ease-out forwards;
    }

    @keyframes draw-circle {
        to {
            stroke-dashoffset: 0;
        }
    }

    @keyframes draw-check {
        to {
            stroke-dashoffset: 0;
        }
    }

    .success-message {
        color: #2E7D32;
        margin-bottom: 0.5em;
        opacity: 0;
        animation: fade-in 1s 0.5s forwards;
        font-size: 1.5rem;
        font-weight: 600;
    }

    .order-details {
        color: #424242;
        margin-bottom: 1.5em;
        opacity: 0;
        animation: fade-in 1s 1s forwards;
        font-size: 1rem;
    }

    @keyframes fade-in {
        to {
            opacity: 1;
        }
    }

    h1 {
        color: #212121;
        font-size: 2rem;
        margin-bottom: 0.5em;
        font-weight: 700;
    }

    p {
        color: #616161;
        line-height: 1.6;
        margin-bottom: 2em;
    }

    .btn-container {
        display: flex;
        justify-content: center;
        gap: 1rem;
        margin-top: 2em;
    }

    .btn {
        padding: 0.75em 1.5em;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 500;
        transition: all 0.2s ease;
    }

    .btn-primary {
        background-color: #1976D2;
        color: white;
    }

    .btn-primary:hover {
        background-color: #1565C0;
    }

    .btn-secondary {
        background-color: #F5F5F5;
        color: #424242;
        border: 1px solid #E0E0E0;
    }

    .btn-secondary:hover {
        background-color: #EEEEEE;
    }
</style>

<div class="success-container">
    <div class="checkmark-container">
        <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
            <circle class="checkmark__circle" cx="26" cy="26" r="22" fill="none"/>
            <path class="checkmark__check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8"/>
        </svg>
    </div>
    <h1>Thank you!</h1>
    <div class="success-message">Order Successfully Placed!</div>
    <p class="order-details">Thank you for your purchase. Your order number is <%= orders._id %>.</p>
    <p>
        We will immediately process your order and it will be delivered in 2-5 business days.
    </p>
    <div class="btn-container">
        <a href="/orders" class="btn btn-primary">My Orders</a>
        <a href="/allProducts" class="btn btn-secondary">Continue Shopping</a>
    </div>
</div>  