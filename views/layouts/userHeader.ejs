<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CoveHive - Premium  Phone</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        black: '#000000',
                        navy: '#14213d',
                        lightGray: '#e5e5e5',
                        white: '#ffffff',
                    }
                }
            }
        }
    </script>
     <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
     <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</head>
<body class="min-h-screen flex flex-col bg-white">
    <nav class="bg-black text-white p-4 sticky top-0 z-10 shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <a href="#" class="text-2xl font-bold">CoveHive</a>
            <div class="hidden md:flex space-x-6">
                <a href="/home" class="hover:text-lightGray transition duration-300">Home</a>    
               </select>
                <a href="/profile" class="hover:text-lightGray transition duration-300">My Account</a>
                <a href="#" class="hover:text-lightGray transition duration-300">My Orders</a>
                <a href="#" class="hover:text-lightGray transition duration-300">Wishlist</a>
                <a href="#" class="hover:text-lightGray transition duration-300"><% if(userName){ %>
                    <%= userName %>
               <% } %></a>
            </div>
            <div class="md:hidden flex space-x-4">
                <i class="fas fa-shopping-bag"></i>
                <i class="fas fa-user"></i>
                <i class="fas fa-heart"></i>
                <i class="fas fa-sign-in-alt"></i>
                <i class="fas fa-bars"></i>
            </div>
        </div>
    </nav>